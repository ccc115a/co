# v0.7 — ch12 OS 在 hackemu 上執行

> 狀態：🚧 進行中（2026-09-10）
> 目標里程碑：**M7 — 把 `12/*.jack` 整包 OS 編譯、開機、執行，並在 hackemu 的
> GUI 上跑起需要 OS 的應用程式（例：Pong），可實際玩。**

## 一句話

把 v0.5 的工具鏈（`jack2vm` → `vm2asm` → `hackasm` → `hackemu`）接到課程
第 12 章：先編譯「OS + 應用程式」的 Jack 原始碼，經多檔 bootstrap
（`SP=256` + `call Sys.init 0`）開機，最後在 hackemu GUI / headless 執行。
若成功，`_eda` 這套工具就能跑完 Nand2Tetris 全專案。

## 現況（2026-09-10 實作結果）

- ✅ **M7.1 全過**：`12/` 的 OS 8 檔編譯後逐支與 C oracle
  （`/tmp/coracle/jack2vm`）**8/8 byte 相同**。修掉了 Screen.jack 的兩個
  官方 bug 之後重新比對仍是 8/8（見下方「官方 bug」）。
- ✅ **M7.2 全過**：裁剪版 OS + 最小 app（`gen/os_src/` + `gen/os/Main.jack`
  → `gen/show.bin`，**17770 words**）在 hackemu headless 驗證：
  - `Memory.poke(200,1234)` → `RAM[200]=1234` ✓
  - `Math.multiply(7,3)=21` 的 `if` 門檻通過 → `RAM[201]=777` ✓
  - `Screen.drawPixel(100,50)` → `RAM[17990]=16` ✓
  - `Screen.drawPixel(0,200)` → `RAM[22784]=1` ✓（位址 = 16384+200×32）
  - `Memory.alloc` 的 heap：`Math.init` 的 `Array.new(16)` 資料 1..32768 完整，
    app 的 `Array.new(3)` 資料 `[5,7,0]` 全對 ✓
  - 穩定停在 `Sys.halt` 的 busy loop，SP 穩定、無腐化。
- ✅ **M7.4 headless 全過**：Pong（`11/jack/Pong/`）+ 裁剪版 OS →
  `gen/pong.bin`，**29544 words ≤ 32K**。headless 驗證：
  - 地板線 rows 238–240 全黑、bat rows 229–236、ball 6×6 矩形。
  - 60M→120M cycles 間 ball 從 y≈206 升到 y≈186 → **ball 會動**、幀在推進。
  - 無窮遊戲迴圈穩定跑，無 panic、無記憶體腐化。
- ⏳ M7.4 GUI 人工互動（方向鍵操控 bat）待驗。
- ⏳ M7.3 逐模組單測（ArrayTest/MemoryTest/…）——多數已被 show/pong 覆蓋，
  列為選做。

## OS 現況盤點（`nand2tetris/12/`）

| 類別 | static | 作用 |
|---|---|---|
| `Array.jack` | 0 | `new`/`dispose`（走 Memory.alloc） |
| `Math.jack` | 1 | 數學運算（`abs`/`sqrt`/`two_to_the`…用 iter 逼近；`multiply`/`divide`/`bit`/`mod`） |
| `Memory.jack` | 6 | `peek/poke/alloc/deAlloc`，freeList 由 **2048** 起 |
| `Output.jack` | 4 | 文字輸出（**字型資料佔 ~16.8K 組語字，32K 殺手**） |
| `Screen.jack` | 6 | `drawPixel/Line/Rectangle/Circle`…直接寫 SCREEN |
| `String.jack` | 0 | 字串、`intValue` 解析 |
| `Keyboard.jack` | 1 | `keyPressed/readChar/readLine/readInt` |
| `Sys.jack` | 0 | `init` → `Main.main` → `halt` |

## 執行鏈路（本版實作）

```
gen/os_src/（裁剪版 OS 8 檔） ──jack2vm──▶ *.vm     11/jack/Pong/ ──jack2vm──▶ *.vm
      └──────────────────────────────┬──────────────────────────────┘
                      vm2asm（多檔，bootstrap call Sys.init 0，label 依函式 scope）
                                       │
                                       ▼
                                     gen/*.asm ──hackasm --bin──▶ gen/*.bin ──hackemu──▶ GUI / headless
```

### 32K ROM 的硬限制（本版最重要的發現）

Hack 的 `@x`（A-instruction）**只有 15 個位址位元**，MSB 是 A/C 判別元：
`@x` 對 x>32767 根本沒有編碼。因此 **ROM > 32768 words 的程式，結構上
無法在 Hack 上執行**——不是模擬器的問題，任何仿製 CPU 皆然。

- 全 OS（8 檔）+ 最小 app 組譯出來 = **35330 words > 32K**，失敗。
  元兇是 `Output.jack` 的 16 進字型 push 資料（~16.8K 組語字）。
- vm2asm 密度 ≈ 35330 / 3422 行 VM ≈ **10.3 文本 word/VM 行**（良好目標 5–6）。
- Nand2Tetris 官方論壇共識：OS 塞 32K 「能做得到、但不強求」
  （在 VM Emulator 跑也是正解）。
- **本版方案 = 裁剪版 OS**：
  - 官方站台：`Array / Keyboard / Math / Memory / Screen / String`（不改）。
  - 自訂 `Sys.jack`：`Memory.init → Math.init → Screen.init → Keyboard.init
    → Main.main`。**Memory.init 必須最先**：`Math.init` 會 `Array.new(16)` 走
    `Memory.alloc`，freeList 未初始化（=0）時 alloc 會腐化記憶體
    （官方 Sys 是 Math 最先、Memory 最後，且官方 Memory.alloc 無 freeList=0
    防呆——官方順序在真工具鏈一樣會壞）。
  - 極簡 `Output.jack` 替身（gen/，非官方）：`moveCursor/printChar/printString/
    printInt/println/init` 全空實作，滿足 Pong 的「記分」呼叫、省 16.8K 字型。
    缺點：文字畫面不出字——這是取捨，完整字型版本以 M7.1 的 8/8 byte 證明為準。
  - 結果：`show.bin` 17770、`pong.bin` 29544，皆 ≤ 32768 ✓。

## 本版與「官方版」的程式碼修正差異

官方 nand2tetris `Screen.jack` 有兩個**知名 bug**（course tools 用 builtin OS
所以不會爆，但在「編譯真 .jack」的鏈路上必爆）：

1. `drawPixel`：`let mask = Math.two_to_the(x & 15));` 多一個 `)` → 嚴格
   compiler（我們的 jack2vm 與 C oracle 皆是）直接編譯失敗。
2. `clearScreen`：`while(i<8192){ let screen[i]=white; }` 忘了 `let i=i+1;`
   → **無窮迴圈**，Pong 一開場就卡死。

本版兩處皆已修（`nand2tetris/12/Screen.jack`）。`Screen.jack.md` companion
已同步。這是示範「官方 OS 也有 bug、修正要動手評估」的絕佳教材。

## 待解問題 / 設計決策

1. ✅ **螢幕方向**：實測 ball/drawPixel 落點與位址公式
   `16384 + y×32 + x/16`（bit15 向右）一致，方向問題解除（v0.6 掃尾誤警報）。
2. ⏳ **`Sys.wait` 忙迴圈**：`wait(50)` 約 1 萬條指令；Pong 每幀多次 →
   GUI 執行很慢。**維持真硬體語意**（不特判），玩家 Speed 開上限即可。
3. ✅ **KBD 語意**：與 OS 預期相符；`Keyboard.readChar` 的按下→放開循環
   不需改核心。
4. ✅ **多檔 byte 相容**：M7.1 已 8/8。又修了 vm2asm 多檔 mode 的
   function label scope（`WHILE_EXP`/`END_` 撞名）——單檔 mode 維持與 C
   逐位元相同（課程節語料有 raw label）。C 不比對的只有 bootstrap + scope。
5. ⏳ **`Memory.loadBin`（選做）**：讓 hdl2rs 的 Computer 也能直接 load `.bin`。

## 步驟拆解（進度）

| 步驟 | 內容 | 驗收 | 狀態 |
|---|---|---|---|
| M7.1 | `jack2vm -o gen/os_vm 12/` 編譯 OS 8 檔；逐支與 C oracle byte 比對 | 8/8 零差異 | ✅ |
| M7.2 | 裁剪版 OS + 最小 app 鏈路 → hackemu headless | RAM 標記全過 + heap 驗證 | ✅ |
| M7.3 | 逐 OS 模組測試（ArrayTest/MemoryTest/…） | headless 無 panic、行為相符 | ⏳（情節被 M7.2/4 覆蓋，選做） |
| M7.4 | Pong 上 GUI（+ headless 先驗） | 29544 words ≤32K、ball/bat/floor 全渲染、ball 動 | ✅ headless / ⏳ GUI 人工 |
| M7.5（選做） | hackemu `--keys <file>` 按鍵腳本，automate Pong 回歸 | 腳本重播可重現畫面 | 🔲 |
| M7.6（選做） | hdl2rs `Memory.loadBin` | `.bin` 直接跑 Computer 模擬 | 🔲 |

## 驗證總表（完成時）

- `bash verify.sh`：既有 e2e 全過 ＋ 新增 v0.7 OS headless 區段
  （grep `RAM[200]=1234`、`RAM[201]=777`、`RAM[22784]=1`）。
- 工具鏈 byte 相容覆蓋：11/jack 11 支（既有）＋ 12/OS 8 支（新增）。
- GUI：Pong 可玩（人工驗收，方向鍵）。

## 產物（`_eda/gen/`）

- `os_src/` 裁剪版 OS 8 檔（6 官方 + 自訂 `Sys.jack` + `Output.jack` 替身）
- `os/` app（`Main.jack`，無 Output 依賴的最小演示）
- `show.{asm,hack,bin}` 17770 words —— M7.2 演示
- `pong.{asm,hack,bin}` 29544 words —— M7.4 演示

## 相關文件

- 工具鏈：`_eda/{jack2vm,vm2asm,hackasm,hackemu}/README.md`
- 版本紀錄：`_doc/v0.5.md`（工具鏈）、`_doc/v0.6.md`（hackemu）
- Path 規劃：`_doc/plan.md`